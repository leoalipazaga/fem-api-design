export { Request } from 'express';
// import * as controller from './user.controller';

describe('user controller', () => {
  it('should create a user', async () => {
    // await controller.createUser(new Request({ body:  }))
    expect(1).toBe(1);
  });
});
